# Database Configuration
# No longer needed at root level — the Python backend uses its own .env in backend/
# This file is kept only for reference

# The Python backend uses SQLite (no external database needed)
# See backend/.env for backend configuration

# Frontend Configuration (Vite)
# The Vite dev server runs on port 5173 and proxies /api to http://localhost:5000
